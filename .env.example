# API mode (pay-per-token). Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Subscription mode uses your Claude login instead — do NOT set an API key for it,
# or it silently bills you per token. See the README for setup.
